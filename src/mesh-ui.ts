import { jsx, html, setHtmlProp } from './parts/jsx';
import { render, displace } from './parts/dom';
import { state } from './parts/state';
import * as checkType from './parts/type-checks';

export {
    jsx, html,
    setHtmlProp,
    render, displace, state,
    checkType    
}

export default {
    jsx, html,
    setHtmlProp,
    render, displace, state,
    checkType    
}